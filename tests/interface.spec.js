import {
    Data,
    View,
    Domain,
} from '../src';

describe('Immview', function() {
    it('exports', function() {
        expect(Data).toBeDefined();
        expect(View).toBeDefined();
        expect(Domain).toBeDefined();
    });
});
